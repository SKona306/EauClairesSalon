@{
  Layout = "_Layout";
}

@using HairSalon.Models;

<h1>Stylists:</h1>

@if(@Model.Count == 0)
{
  <p>There are currently no Stylists in the system.</p>
}

<ul>
  @foreach(Stylist stylist in Model)
  {
    <li>stylist.Name</li>
  }

</ul>